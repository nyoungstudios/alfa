[common]
setup-tag = "setup"

[case.default]
os = "ubuntu-24.04"

[case.default.test]
assert-install-names = ["run_command", "snap_package"]

[[case.default.test.commands]]
command = "bash"
arguments = ["-ceu", "cat /tmp/snap_calls.log"]
assert-stdout-contains = "install kubectl --classic"
