[common]
setup-tag = "setup"

[case.default]
os = "ubuntu-24.04"

[case.default.test]
assert-install-names = ["run_command", "sdk_candidate"]

[[case.default.test.commands]]
command = "bash"
arguments = ["-ceu", "cat /tmp/sdk_calls.log"]
assert-stdout-contains = "install java 21.0.2-tem"
