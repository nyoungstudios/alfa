[ run_command ]
tags = ["setup"]
options = [
  "bash",
  "-ceu",
  "mkdir -p ~/.nvm && printf 'nvm() { echo "$@" >> /tmp/nvm_version_calls.log; }\n' > ~/.nvm/nvm.sh && : > /tmp/nvm_version_calls.log"
]

[ node_version ]
options = [
  "20.11.1"
]
